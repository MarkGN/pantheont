[h:"Make sure to deselect 'apply to selected tokens' under the Details menu!"]

[h: names=getTokenNames(",")]
[h,FOREACH(name, names), code: {
	[h:test=startsWith(name,"light")]
	[h,if(test),code:{
		[h:setLight("D20", "Candle - 5", 1, name)]
	}
	]
}
]